import { ScrollProgressBarDirective } from './scroll-progress-bar.directive';

describe('ScrollProgressBarDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollProgressBarDirective();
    expect(directive).toBeTruthy();
  });
});
